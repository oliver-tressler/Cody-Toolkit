{{ model.logical_name }}: new LookupAttribute([{{- children -}}], "{{model.logical_name}}", Attribute.AttributeType.{{model.type}}, "{{ model.schema_name }}", "{{ model.odata_name }}"{{ if model.display_name }}, "{{ model.display_name }}"{{ end }}),