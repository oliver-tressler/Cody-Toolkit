constructor({{if model.target_action }}target: EntityReference{{ end }}){
    super({ actionName: "{{ model.name }}", argumentMetadata, {{ if model.target_action }}logicalName: "{{ model.primary_entity }}"}, target{{ else }} } {{ end }});
}